import React from 'react';
import { Target } from 'lucide-react';

const ProjectsTab = () => {
  return (
    <section className="tm-placeholder">
      <Target size={18} />
      <div>
        <h3>Projects view is ready for extension</h3>
        <p>This section can be wired to live APIs while keeping the same Team Management shell.</p>
      </div>
    </section>
  );
};

export default ProjectsTab;
